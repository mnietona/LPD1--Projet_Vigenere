/**
 * @author Nieto Navarrete Matias
 * matricue : 502920
 */
#include "Decriptage.hpp"


/**
 * @brief Lit le fichier texte et le stocke dans un string
 * 
 * @param nom_fichier le nom du fichier a lire
 * @param texte le string qui contiendra le fichier
 */
void lecture_fichier(const string &nom_fichier, string &texte);
/**
 * @brief ecrit dans un fichier le texte
 * 
 * @param nom_fichier le nom du fichier a ecrire
 * @param texte le texte a ecrire
 */
void ecriture_fichier(const string &nom_fichier, const string &texte);


